/* ==================================================================
   결정적 난수 — 같은 시드면 같은 결과가 재현된다.

   battle.js 안에 두었더니 장비·룬 모듈이 이걸 쓰려고 battle 을
   불러오고, battle 은 다시 장비를 불러오는 순환 참조가 생겼다.
   의존이 한쪽으로만 흐르도록 따로 뺐다.
   ================================================================== */
export function makeRng(seed) {
  let a = (seed >>> 0) || 1
  return () => {
    a |= 0; a = (a + 0x6D2B79F5) | 0
    let t = Math.imul(a ^ (a >>> 15), 1 | a)
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296
  }
}
