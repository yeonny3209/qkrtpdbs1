{
  "name": "day100",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "oxlint src test",
    "test": "tsx test/logic.test.ts",
    "check": "oxlint src test && tsc --noEmit && npm run test && npm run test:render",
    "test:render": "tsx test/render.test.ts"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "oxlint": "^1.10.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
